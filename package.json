{
  "name": "llm-local",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:web": "pnpm --filter @llm-local/web dev",
    "dev:api": "pnpm --filter @llm-local/api dev",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @llm-local/web build",
    "build:api": "pnpm --filter @llm-local/api build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
