{
  "name": "tadzuna",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:web": "pnpm --filter @tadzuna/web dev",
    "dev:api": "pnpm --filter @tadzuna/api dev",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @tadzuna/web build",
    "build:api": "pnpm --filter @tadzuna/api build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
